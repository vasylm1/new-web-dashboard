# Core app
streamlit>=1.58.0,<2.0

# Data / spreadsheets
pandas>=3.0.3,<4.0
openpyxl>=3.1.5,<4.0
XlsxWriter>=3.2.9,<4.0

# QR codes & images
qrcode>=8.2,<9.0
pillow>=12.2.0,<13.0

# Document conversion
# Only the extras the MD tool actually uses (drops heavy audio/Azure extras
# from [all] to fit Streamlit Community Cloud's 1 GB free tier).
markitdown[docx,pptx,xlsx,xls,pdf,youtube-transcription]>=0.1.6,<0.2
PyPDF2==3.0.1
# Markdown -> HTML tool
markdown>=3.10.2,<4.0
# HTML -> Markdown tool
markdownify>=1.2.2,<2.0

# Charts, SQL formatting, sample data
matplotlib>=3.11.0,<4.0
sqlparse>=0.5.5,<0.6
Faker>=40.23.0,<41

# PDF documents (resume, certificate, labels, watermark) and EPUB
fpdf2>=2.8.7,<3.0
EbookLib>=0.20,<1.0

# Text-to-speech (PDF -> MP3)
gTTS>=2.5.4,<3.0
